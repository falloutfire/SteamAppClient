# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '12.0'

target 'SteamApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SteamApp
  pod 'RouteComposer'
  pod 'Kingfisher', '~> 7.0'
  pod 'Alamofire', '~> 5.4'

  target 'SteamAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SteamAppUITests' do
    # Pods for testing
  end

end
